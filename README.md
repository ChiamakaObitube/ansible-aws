## Ansible starter file to provision setup a NodeJS web server in EC2 instance

### Command to run Ansible playbook
```ansible-playbook main-remote.yml -i inventory --private-key <path to private key> ```
